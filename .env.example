DATABASE_URL=postgresql://postgres:postgres@localhost:5432/faw
PAYLOAD_SECRET=cambia-este-secreto-local
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Storage (local | azure)
STORAGE_PROVIDER=local

# Azure Blob (when STORAGE_PROVIDER=azure; never commit the real secret)
AZURE_STORAGE_CONNECTION_STRING=
AZURE_STORAGE_CONTAINER=faw-media
AZURE_STORAGE_ACCOUNT_BASEURL=https://gpdigitalsa.blob.core.windows.net
